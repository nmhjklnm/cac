---
title: "cac"
description: "Isolate, protect, and manage your Claude Code"
---

Manage Claude Code versions, environments, identities, and proxies from a single CLI. Each environment is fully isolated: its own Claude Code version, `.claude` config, device fingerprint, and network route.

<Columns cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Install cac and create your first environment in 60 seconds.
  </Card>
  <Card title="Concepts" icon="lightbulb" href="/concepts">
    Understand how versions, environments, and isolation work.
  </Card>
  <Card title="Commands" icon="terminal" href="/commands/env">
    Full command reference for cac claude, env, self, and docker.
  </Card>
  <Card title="How it works" icon="shield" href="/reference/how-it-works">
    Wrapper architecture, fingerprint spoofing, and telemetry blocking.
  </Card>
</Columns>

## Why cac?

Claude Code reads device identifiers at runtime — hardware UUID, MAC address, hostname, machine ID. It also phones home to telemetry endpoints. **cac** intercepts all of this at the process level, without modifying Claude Code's source.

| What cac does | How |
|:---|:---|
| Version management | Download, pin, and switch Claude Code binaries |
| Config isolation | Each environment gets its own `CLAUDE_CONFIG_DIR` |
| Fingerprint spoofing | Shell shims + Node.js hooks replace all device identifiers |
| Telemetry blocking | DNS guard + 12 env vars + fetch interception |
| Proxy routing | Process-level HTTP/SOCKS5 with auto-protocol detection |
| TUN bypass | Auto-relay when TUN proxy software is detected |

## Command overview

```bash
cac claude   install | ls | pin | uninstall       # version management
cac env      create | ls | rm | activate |         # environment management
             deactivate | check
cac self     update | delete                       # self-management
cac docker   setup | start | enter | ...           # containerized mode
```
